{% extends 'base.html.twig' %}

{% block title %}{{'Page Produit'| trans}}{% endblock %}

{% block body %}
<div class="container">
  <h1>{{'Liste de Produit'| trans}}</h1>
    <hr>

    {% if produits|length >0 %}
      {% for produit in produits  %}
        <div class="row">
        <div class="col-4">
          <div class="card" style="width: 18rem;">
            {% if produit.photo is not null %}
              <img  class="card-img-top" src="{{ asset('assets/images/' ~ produit.photo) }}" style="height:18rem"/>
            {% endif %}
            <div class="card-body">
              <h5 class="card-title">{{ produit.nom }}</h5>
              <a href="{{ path('produit',{id :produit.id}) }}" class="btn btn-dark">{{'voir produit'| trans}}</a>
            </div>
          </div>
        </div>
    </div>
      {% endfor %}
    {% else %}
      <p>{{"il n'y a pas encore de produit"| trans}}</p>
    {% endif %}
  <br>
    <div class="card bg-light" style='padding: 20px'>
      {{ form(formprod) }}
    </div>
</div>
{% endblock %}
